# 介面契約 — M4 README + 使用者導引

> 協調者產出的契約。文件角色照本契約撰寫；不動程式邏輯。
> 最後更新：2026-06-06。

## 0. 目標與非目標

**目標**：把現有 README 從「技術視角」改寫為「**使用者面陳述 + 開發者指令**」，讓三類讀者各取所需：
1. **HR 使用者**：這是什麼工具？解決我什麼問題？怎麼開始用？
2. **開發者**：技術棧、開發指令、文件入口。
3. **新接手的維護者**：路線圖、收尾報告。

**v1 非目標**：
- 完整使用者手冊（README 是入口，深入操作交給「規劃旅程 walkthrough」一節 + 應用內總覽頁）
- 截圖／影片（M5 規格才考慮）

## 1. 結構

README 由上而下：

1. **標題 + 一句話定位**：HR 組織規劃工具，沿匯報線 × 專案職能雙維度規劃組織。
2. **這個工具給誰／解決什麼**（小段）：對齊 [[product-vision-hr-planning]] 校準後定位（非全公司營運系統）。
3. **規劃旅程 walkthrough**（**新增、核心**）：6 步——載入 → 編輯 → 健檢 → 比較 → 影響 → 發布，每步：
   - 在做什麼
   - 對應頁面（連結到應用內路由說明）
   - 一句話例子
4. **核心功能**（重寫）：以「**HR 規劃主線**」+「**基礎能力**」分組。
5. **技術棧**（保留簡化）
6. **快速開始**（保留：純前端 / Docker compose）
7. **專案結構**（保留簡化）
8. **資料與匯入**（保留 CSV/xlsx 與資料格式）
9. **指令**（保留）
10. **開發協作流程**（簡述 + 連結）
11. **進度與路線圖**：M1–M4 已完成、降級項目；連到待辦清單。
12. **文件導覽**：**只連活的文件**——契約們、系統設計文件、待辦清單、CLAUDE.md、工作流程；歷史報告連 `docs/reports/`。

## 2. 必須更新的點

- 移除舊路線陳述（「後續擴充」段提到的送簽/SSO/多租戶/P4/P5/P6 全部歸入「已降級」並附定位校準理由連結）。
- 移除文件導覽中**死連結**：
  - `docs/規劃-後端與持久層.md` 已搬到 `docs/reports/`
  - `docs/規劃-自動化測試與schema版控.md` 已搬到 `docs/reports/`
  - `docs/無障礙稽核報告.md`、`docs/UI測試驗收報告.md` 已搬到 `docs/reports/`
- 更新 testing 段中的「255 測試（43 檔）」為現況數字（驗收前最後一次跑 verify 抓 count；若不方便就改為「~360 測試」）。
- 「組織圖（兩種視角）」描述補上**雙維度過濾**與職能視角面板。
- 加上**新總覽頁**為入口的說明。
- 「資料版本」段補上**生效日**「排程／已生效」徽章說明（既有但可強化）。

## 3. 不必更新的點

- 既有技術棧表
- Docker compose 指令
- CSV 欄位表
- npm scripts 表

## 4. 角色分派

| 角色 | 範圍 |
|---|---|
| 協調者 | 本契約、整合驗收（檢查連結） |
| 文件（Technical Writer） | 重寫 README.md |
| QA／審查 | 不派（純文件無邏輯）|

**驗收**：
- 所有 markdown 連結可達（協調者驗）。
- 包含 §1 全部 12 章節順序。
- `npm run verify` 不受影響（純 .md 改動）。
