# 代辦清單（Backlog）

> 由 Claude 在 `integration` 分支逐項完成；每項完成即推送（pre-push 驗證把關）。
> 最後更新：2026-06-07（**層級改由「主匯報深度」自動計算、`assignment.level` 退為手動覆寫**完成：先前組織圖層帶／健檢層級深度／BPMN 申請人層級直接吃手填的 `assignment.level`，數值常與真實匯報關係不一致（主管與部屬同層、根不是第 1 層），組織圖看不出階層、健檢深度也算錯。修正後**層級＝主匯報深度，自動計算**（根＝第 1 層、每階 +1、主管必在部屬上一層；沿 `primarySupervisorId`、忽略虛線/次要主管），新增純函式 `src/services/reportingDepth.ts`（`computePrimaryDepth`／`effectiveLevel`）為**單一真實來源**，`buildOrgFlowGraph`／`orgHealth`／`bpmnSimulator` 三處共用。`assignment.level` 退為**稀疏的手動覆寫**（預設不設值走自動計算、刻意垂直拖曳才寫單點覆寫且不連動子樹）；drag-to-reassign 會清掉層級覆寫讓人跟隨新主管下一層；**migration v2→v3（`toV3`，`ORG_SCHEMA_VERSION=3`）清空舊資料殘留 level**。佈局視覺可能仍需 1 輪看圖微調，但層級數字與「主管在部屬上一層」已到位；只改前端、後端零改動；契約見 [契約-階層由匯報深度計算](契約-階層由匯報深度計算.md)。前項：**組織圖工作台（/workbench）佈局直觀化 + 預設全公司視角**完成：`/workbench` 進頁預設用全公司視角顯示整體組織（`/org-chart` 不變）；`buildOrgFlowGraph.ts` 佈局管線重寫＝`resolveLevels → layoutWithDagre（跨層邊插 dummy 中繼保留水平通道、dummy 不外洩）→ centerParents（父水平置中於直接部屬）→ applyLevelBands`，讓跨層匯報線不被中間層節點遮蔽、階層一眼可辨；smoothstep `pathOptions` 把跨層水平段下沉到層間空隙；拖曳吸附／層級輔助線／drag-to-reassign 不破壞；只改前端、後端零改動；佈局視覺驗收靠看圖、可能 1–2 輪迭代；契約見 [契約-工作台佈局與預設視角](契約-工作台佈局與預設視角.md)。前項：**組織圖工作台（整合主介面）階段 3——drag-to-reassign（拖拉改匯報線）**完成：在組織圖（reporting 編輯）把某人節點拖到另一名員工節點上＝改其主管匯報線；新增純函式 `reassignSupervisor`（`orgOperations.ts`，含循環防護／保留 dotted／走 `validateAssignment`），`OrgFlowChart.onNodeDragStop` 以 `getIntersectingNodes` 偵測 drop target（命中改主管＋失敗回滾＋短暫 Alert，未命中走既有 level 邏輯）、`onNodeDrag` 以 `isDropTarget` hover 高亮（所見即所得）；連線／詳情面板為鍵盤替代保留；`/workbench` 與 `/org-chart` 兩頁共用畫布同時生效。後端零改動。四階段（階段 4 流程衝擊摘要 + 情境局部入口待做）。前端約 523 個測試）。
> 進度：M1 ✅ → M2 ✅ → M3 ✅ → M4 ✅ → M5 ✅ → Phase 0 ✅ → Phase 5 🔧（R5.2 ✅、R5.3 ✅）→ Phase 4 🔧（R4.3 ✅）→ **組織圖工作台 🔧（階段 1 ✅、階段 2 ✅、階段 3 ✅）** ‖ Roadmap Phase 1-5 剩項 ⏳ 等訪談。

## 定位（最高指導原則）

本工具為 **HR 規劃／試算工具**（匯報線 × 專案職能），不是全公司營運系統。
BPMN 是組織變更影響的**輔助透鏡**，非簽核引擎。
偏全公司營運系統的項目（送簽、SSO、多租戶、後端正規化）已**降為非主線**。

---

## 進行中：🔧 組織圖工作台（整合主介面，四階段；階段 1、2、3 完成）

**這是已批准、不依賴訪談的大功能，正在實作中**——願景為「以組織圖拖拉為中心的整合主介面，把各功能即時嵌入、需要細節才引導出去」。
與下方「等訪談」狀態**並存且互不衝突**：訪談針對的是 Roadmap Phase 1–5 的**待驗證假設**；組織圖工作台是已決策的主介面方向，照契約分階段交付。契約見 **[契約-組織圖工作台](契約-組織圖工作台.md)**（協調者產出，各角色照契約開發、不自改契約）。

**架構決策**：抽 `useOrgFlowEditing()`（把 `OrgChartPage` 的編輯 session 編排抽出）→ 新建 `/workbench` 用同一 hook 換版面 → **保留 `/org-chart` 當對照與回滾**。`OrgFlowChart` 畫布已 props-driven、不複製。後端零改動。

四階段（各自走 6+4 管線、可獨立驗收、可回滾）：

- ✅ **階段 1 — 地基**（完成）：抽共用 hook `useOrgFlowEditing`（`src/hooks/useOrgFlowEditing.ts`）+ 抽共用元件 `FindingGroup`（`src/components/orgHealth/FindingGroup.tsx`）+ 新建 `WorkbenchPage`（`/workbench`）reporting 視角編輯骨架（與 `/org-chart` 匯報視角等價：進編輯／拖層級／連線改主管／詳情面板／檢查點／發布／`EditImpactBar` 即時 delta／`SnapshotPanel`／staleDataWarning）+ `App` 路由 + `Layout` 導覽「工作台」（icon `LayoutPanelLeft`）。`OrgChartPage` 改用同一 hook、**行為等價（既有測試全綠不需改斷言）**；`OrgHealthPage` 改 import 共用 `FindingGroup`。
- ✅ **階段 2 — 即時整合面板**（完成，commit `c530f3e`）：`/workbench` 右側新增可摺疊**即時整合面板 `WorkbenchInsightPanel`**（`src/components/orgFlow/`）——**規劃就緒度徽章**（`buildReadiness`）+ **即時 findings**（`buildOrgHealth(orgData).findings` 用共用 `FindingGroup` 依 category 分群；**CM 防過載**＝預設只顯示 warning，提供「展開全部（含 N 則提示）⇄只看警示」切換）+ **職能覆蓋**（`FunctionCoveragePanel`），即時反映編輯中草稿。**選中組織圖節點** → 面板頂部顯示「{姓名} 的相關提醒」（findings 縮限到該人，新增純函式 **`filterFindingsForEmployee`**（`src/services/orgHealth.ts`）），就緒度／職能維持全域。底部**引導連結區 `WorkbenchGuideLinks`** 導向 `/people`／`/groups`／`/compare`／`/bpmn/impact`／`/csv-import`。**階段 2.0 先還技術債**：抽共用 **`ReportingEditCanvas`** + **`orgFlowGroupSelection`**（`pickDefaultGroupId`／`resolveGroupId`），`OrgChartPage` 與 `WorkbenchPage` 共用 reporting 編輯版面與組別選擇（去重）、**OrgChartPage 行為等價（既有測試全綠不改斷言）**。後端零改動。
- ✅ **階段 3 — drag-to-reassign**（完成，commit `7e5a066`）：在組織圖（reporting 編輯）**把某人節點拖到另一名員工節點上＝改其主管匯報線**。新增純函式 **`reassignSupervisor(data, assignmentId, newSupervisorId, operator)`**（`src/services/orgOperations.ts`）——設 `primarySupervisorId = newSupervisorId`、`supervisorIds` 去舊主主管並確保含新主管、**保留其餘 dotted 主管**，走既有 `upsertAssignment`（含 `validateAssignment` 擋自我／inactive／找不到員工組別），**循環防護**＝套用後以 `detectReportingCycleFromAssignments` 檢查整體匯報關係，有循環則整筆不套用回 `error`（回傳 `{ data, error }`，失敗時 `data` 為原資料未變動）。`OrgFlowChart.onNodeDragStop` 以 React Flow v12 **`getIntersectingNodes`** 偵測 drop target（過濾自己與 `ghost-*`）：**命中員工節點**＝改主管（成功 `onDraftChange` 重繪、由 dagre 重佈局；失敗＝**回滾被拖節點視覺位置** + 短暫 `Alert` 顯示原因），**未命中**＝走既有 level（層級）邏輯（完全保留、等價）；`onNodeDrag` 以 **`isDropTarget` hover 高亮**可放置目標，drop 採 hover 當下已算的 target（**所見即所得**）。**鍵盤替代**：拖拉無鍵盤替代，但**既有連線（`onConnect`）與詳情面板（`OrgDetailPanel` 編 `supervisorIds`）為鍵盤可達等效路徑**，保留不退化。**`/workbench` 與 `/org-chart` 兩頁共用同一畫布、drag-to-reassign 同時生效**。後端零改動。
- ⏳ **階段 4 — 延伸角度**：流程衝擊摘要 + scenario 局部比較入口。

---

## 下一個開工（Roadmap 剩項）：⏳ 等待真實 HR 訪談結果（不再賭假設）

**「不賭假設」的低風險池已收乾淨**——Phase 0 vibe 校準（`1bdd955`）、Phase 5 R5.2（`8d72028`）／R5.3（`086e4b3`）、Phase 4 R4.3（`e0b3636`）皆已落地（見下方「已完成」段）。
這些是**不依賴外部驗證、低風險、高把握**的改善；如今它們都做完了。

**剩下的項目幾乎都是「待驗證假設」，再往下做就是在賭未經驗證的需求。** 因此下一步不是直接開工某個 Rx.x，
而是**先訪談 3–5 位真實 HR**，用訊號決定要做哪些、怎麼做。訪談工具包（題目、流程、紀錄模板）見
**[訪談-HR驗證指南](訪談-HR驗證指南.md)**；8 個關鍵驗證問題見 [規劃-改善Roadmap §9](規劃-改善Roadmap.md)。

> **為什麼停在這裡**：再往下做的每個 Phase 都帶著「HR 真的要這個嗎／要的是這個形式嗎」的未驗證假設。
> CM 評估 Desire 階段（變革失敗第一名原因）目前只覆蓋約 5%，但**用什麼形式補**必須先聽真實 HR，
> 否則容易做出「自以為對」的功能。先小投資訪談、再依訊號決定，比直接賭大投資划算。

### 各候選的訪談相依（訪談前皆不動）

完整 Phase 1–5 與觀察項詳見 **[規劃-改善Roadmap](規劃-改善Roadmap.md)**；系統內 `/roadmap` 頁面同步呈現。

- **Phase 1 — Desire／Stakeholder**（R1.1／R1.2／R1.3）：補 ADKAR 的 Desire 階段（發布版本四段狀態、CEO/Sponsor Brief、Stakeholder Map）。**須先訪談問題 #1／#2／#6**（發布語意、Sponsor Brief 形式、stakeholder mapping 工具現況）。
- **Phase 2 — Reinforcement／Sustainment**（R2.1）：補變革後的追蹤鞏固。**須先訪談驗證**（HR 是否真的追蹤、追什麼）。
- **Phase 3 — 流程衝擊強化**：BPMN 透鏡的深化。**須先訪談驗證**（HR 是否願意維護流程定義、想看什麼衝擊）。
- **Phase 4 — 規劃模型擴充**：R4.3 ✅ 已落地（低風險、不賭假設）；**R4.1／R4.2 屬較大模型擴充，須先訪談驗證**再動。
- **Phase 5 — 體驗摩擦消除**：R5.2 ✅／R5.3 ✅ 已落地；**其餘 R5.1／R5.4／R5.5 本身是 UXR 待驗證假設**，須觀察真實使用摩擦或訪談訊號再動。

> **紅線提醒**：訪談前不要憑想像把上述任一項當「確定要做」開工。先收訊號，再排序。題目見 [規劃-改善Roadmap §9](規劃-改善Roadmap.md)、流程見 [訪談-HR驗證指南](訪談-HR驗證指南.md)。

---

## 已完成（按時間倒序，細節在系統設計文件與契約文件）

### HR 規劃主線
- ✅ **層級改由「主匯報深度」自動計算、`assignment.level` 退為手動覆寫**（契約見 [契約-階層由匯報深度計算](契約-階層由匯報深度計算.md)）——修正「層級數字與真實匯報關係不一致（主管與部屬同層、根不是第 1 層）」的根因，只改前端、後端零改動
  - **層級＝主匯報深度，自動計算**：根（無主管者）＝第 1 層、每往下一階 +1、主管必在部屬上一層；沿 `primarySupervisorId`（主匯報）計算、**忽略虛線/次要主管**（不被矩陣關係污染）。組織圖層帶、健檢層級深度、BPMN 申請人層級改用這個**單一真實來源**
  - **新純函式服務** `src/services/reportingDepth.ts`：`computePrimaryDepth(assignments)` 算每位員工的主匯報深度（1-indexed、以員工為單位、正規父＝顯示歸屬的 `primarySupervisorId`、含防環）、`effectiveLevel(assignment, depthMap)` 取有效層級（**覆寫優先、否則用計算深度**）；`buildOrgFlowGraph`（層帶／`resolveLevels` 移除 dagre-rank 後備）、`orgHealth`（層級深度，不再 backfill）、`bpmnSimulator`（`requesterLevel`）三處共用
  - **`assignment.level` 退為「手動覆寫」**：預設不設值（走自動計算）；只有使用者**刻意垂直拖曳**某節點時才寫入覆寫值（覆寫優先於計算深度、**單點不向下連動子樹**）
  - **改主管自動跟隨**：drag-to-reassign（拖人到新主管節點）會**清掉其層級覆寫**，讓他自動落到新主管下一層（「換了主管、層級自然跟著走」）；改在 `reassignSupervisor`（`orgOperations.ts`）內處理
  - **資料升級 migration v2→v3**：`ORG_SCHEMA_VERSION` 升為 3、新增 `toV3` 把舊資料殘留的 `level` 一律清空（既存值多為佈局產物／職等殘值、非使用者刻意覆寫），之後才由垂直拖曳產生真正的稀疏覆寫；歷史 `toV1` 的 `backfillAssignmentLevels` 不動（其回填值會被 `toV3` 清除）
  - **佈局視覺可能仍需 1 輪看圖微調**（offset／置中），但**層級數字與「主管在部屬上一層」已到位**
  - 後端零改動
- ✅ **組織圖工作台（/workbench）佈局直觀化 + 預設全公司視角**（契約見 [契約-工作台佈局與預設視角](契約-工作台佈局與預設視角.md)）——只改前端佈局與預設視角、不動契約／後端
  - **預設全公司視角**：`/workbench` 進頁預設用「**全公司視角**」（`ALL_GROUPS_VIEW_ID`）顯示整體組織，而非預設挑某個組別（reporting／membership 皆從全公司起）。範圍僅 `WorkbenchPage`（整合主介面）；**`/org-chart` 維持 `pickDefaultGroupId` 不變**（對照頁，行為與既有測試不動）
  - **匯報線佈局直觀化**：解決「跨層匯報線被中間層節點遮蔽、看不出階層」的問題（例：某 L3 主管的 L4／L5 部屬之前看不出來）。`buildOrgFlowGraph.ts` 佈局管線重寫為 `resolveLevels → layoutWithDagre → centerParents → applyLevelBands`：
    - **(a) 跨層邊插 dummy 中繼節點**：對「target 與 source 層級差 > 1」的邊插入 dummy 中繼節點鏈，讓 Dagre 為跨層邊在中間層保留水平通道、真實節點自動避讓，且真實節點 rank 對齊其層級；**dummy 只影響排版、不外洩到輸出 nodes**
    - **(b) 父置中於直接部屬**：依主匯報邊（`edge.data.isPrimary`）把父節點 X 置中於其直接部屬範圍中心（org chart 慣例，由葉往根逐層收斂）——同一主管的部屬聚集在父下方
    - **(c) 層間路由**：edge 用 smoothstep `pathOptions`（borderRadius 12、offset 半個 RANK_SEP），讓跨層水平段下沉到層與層之間的空隙
    - 拖曳吸附（`levelFromTopY`／`LEVEL_GAP`）、層級輔助線、drag-to-reassign 既有機制**不受破壞**
  - 後端零改動。**佈局視覺驗收靠看圖，可能需 1–2 輪看圖迭代**（契約已載明）
- ✅ **組織圖工作台（整合主介面）階段 3 — drag-to-reassign（拖拉改匯報線）**（commit `7e5a066`，契約見 [契約-組織圖工作台](契約-組織圖工作台.md) §3）——在組織圖（reporting 編輯）**把某人節點拖到另一名員工節點上＝改其主管匯報線**，改點集中在 `OrgFlowChart` 的拖拉手勢、兩頁共用畫布同時生效
  - **新純函式** `reassignSupervisor(data, assignmentId, newSupervisorId, operator)`（`src/services/orgOperations.ts`）：設 `primarySupervisorId = newSupervisorId`、`supervisorIds` 去舊主主管並確保含新主管、**保留其餘 dotted（虛線）主管**，走既有 `upsertAssignment`（含 `validateAssignment`：擋自我指派／inactive／找不到員工組別）；**循環防護**＝套用後以 `detectReportingCycleFromAssignments` 檢查整體匯報關係，有循環則**整筆不套用**回 `error`；回傳 `{ data, error }`（`error === null` 才成功，失敗時 `data` 為**原資料未變動**）
  - **`OrgFlowChart.onNodeDragStop` 擴展**：以 React Flow v12 `getIntersectingNodes` 偵測 drop target（過濾自己與 `ghost-*`，取最上層員工節點）——**命中**＝呼叫 `reassignSupervisor` 改主管（成功 `onDraftChange` 重繪、由 dagre 重佈局；失敗＝**回滾被拖節點視覺位置** + 短暫 `Alert` 顯示原因），**未命中**＝走既有 level（層級吸附）邏輯（完全保留、等價）
  - **hover 高亮**：`onNodeDrag` 偵測 intersecting target、以 `isDropTarget` 高亮可放置目標；drop 採 hover 當下已算的 target（**所見即所得**）
  - **鍵盤替代保留**：拖拉本身無鍵盤替代，但**既有連線（`onConnect`）與詳情面板（`OrgDetailPanel` 編 `supervisorIds`）為鍵盤可達等效路徑**，明訂保留不退化（契約 §3.4 a11y 替代）
  - **`/workbench` 與 `/org-chart` 兩頁共用同一 `OrgFlowChart` 畫布，drag-to-reassign 同時生效**；OrgChartPage level 拖拉等價（既有測試全綠）
  - 後端零改動
  - 階段 4 待做：流程衝擊摘要 + scenario 局部比較入口
- ✅ **組織圖工作台（整合主介面）階段 2 — 即時整合面板**（commit `c530f3e`，契約見 [契約-組織圖工作台](契約-組織圖工作台.md) §2）——把 `/workbench` 從「編輯骨架」升級為「整合主介面」：編輯／檢視時右側即時呈現各角度提醒、選中節點看該人提醒、需要細節時引導出去
  - **新元件** `WorkbenchInsightPanel`（`src/components/orgFlow/`）：右側**可摺疊**即時整合面板（`<aside aria-label>` landmark、摺疊鈕 `aria-expanded` 同步），即時反映編輯中草稿（`buildOrgHealth(orgData)` 單一 memo）——**規劃就緒度徽章**（`buildReadiness`，level→變體/標籤）+ **即時提醒**（findings 以共用 `FindingGroup` 依固定 category 分群）+ **職能覆蓋**（重用 `FunctionCoveragePanel`）
  - **CM 防過載**：findings 預設**只顯示 warning**，提供「**展開全部（含 N 則提示）⇄只看警示**」切換才露出 info 級提示，避免一次資訊過載
  - **選中節點 → 該人相關提醒**：選定員工時面板頂部改顯示「{姓名} 的相關提醒」、findings 縮限到該人；就緒度／職能覆蓋維持全域（縮限到單人無意義）。`selectedEmployeeId` lift 到 `WorkbenchPage`（經 `ReportingEditCanvas` 的 `onNodeSelect`）
  - **新純函式** `filterFindingsForEmployee(findings, employeeId)`（`src/services/orgHealth.ts`）：過濾 `finding.employeeId === employeeId`；`employeeId` 為 null（未選中）回空陣列由呼叫端改顯示全域；不改輸入順序與內容
  - **新元件** `WorkbenchGuideLinks`（`src/components/orgFlow/`）：底部「需要細節？」引導區，連向 `/people`／`/groups`／`/compare`／`/bpmn/impact`／`/csv-import`，各附一句說明
  - **階段 2.0 還技術債（去重）**：抽共用元件 `ReportingEditCanvas`（`src/components/orgFlow/`，收斂 staleWarning＋`EditModeToolbar`＋`EditImpactBar`＋`OrgFlowChart`(reporting)＋`SnapshotPanel` 與面板開合/重置邏輯）+ 純函式 `orgFlowGroupSelection`（`pickDefaultGroupId`／`resolveGroupId`）；`OrgChartPage` 與 `WorkbenchPage` 共用、**OrgChartPage 行為等價（既有測試全綠不改斷言）**
  - 後端零改動
  - 階段 3 drag-to-reassign 已完成（見上）；階段 4 待做：流程衝擊摘要 + scenario 局部比較入口
- ✅ **組織圖工作台（整合主介面）階段 1 — 地基**（契約見 [契約-組織圖工作台](契約-組織圖工作台.md)）——以組織圖拖拉為中心的整合主介面的第一階段；建立可完整編輯+發布的獨立組織圖頁，作為後續階段疊加面板的容器
  - **新共用 hook** `useOrgFlowEditing`（`src/hooks/useOrgFlowEditing.ts`）：把組織圖「編輯 session 編排」抽成共用 hook（`useEditSession`、staleDataWarning、orgData 解析、base/draft/impactDelta memo、diffResult、enter/exit/saveCheckpoint/publish/onDraftChange 等 handlers）；`OrgChartPage` 改用此 hook、**行為等價（既有測試全綠不需改斷言）**，memo 依賴細化保留避免 stale
  - **新共用元件** `FindingGroup`（`src/components/orgHealth/FindingGroup.tsx`）：從 `OrgHealthPage` 抽出 findings 分群摺疊元件（沿用 details/summary + a11y）；`OrgHealthPage` 改 import 共用元件、行為等價
  - **新頁面** `WorkbenchPage`（`src/pages/WorkbenchPage.tsx`，路由 `/workbench`）：組織圖工作台骨架，**階段 1 為 reporting 視角 + 完整編輯**——進編輯／拖層級／連線改主管／`OrgDetailPanel` 詳情面板／儲存檢查點／`EditModeToolbar` 發布／`EditImpactBar` 即時 delta／`SnapshotPanel`／staleDataWarning，與 `/org-chart` 匯報視角等價但獨立成頁
  - **路由與導覽**：`App.tsx` 加 lazy 路由 `/workbench`；`Layout.tsx` 主導覽加「工作台」（icon `LayoutPanelLeft`）。**`/org-chart` 保留**作為對照與回滾
  - 後端零改動
  - 後續：階段 2 右側即時面板、階段 3 drag-to-reassign 已完成（見上）；階段 4 流程衝擊待做
- ✅ **Phase 4 R4.3 版本命名 + 變更理由**（commit `e0b3636`，契約見 [契約-R4.3-版本命名與理由](契約-R4.3-版本命名與理由.md)、規劃見 [規劃-改善Roadmap §5](規劃-改善Roadmap.md)）——發布版本可填**自訂名稱**與**調整理由**，讓 HR 回顧時知道「這個方案叫什麼、為什麼這樣調」；聚焦版本層級（發布時填一次），不侵入每筆 changelog
  - `R4.3` **版本命名 + 變更理由**：發布版本可填**版本名稱（label）**與**調整理由（note）**，皆為**版本層級 metadata、發布時填一次**
  - **型別**：`PublishedVersion`（`src/services/publishedVersions.ts`）／`DataVersionInfo`（`src/services/dataVersions.ts`）新增 `note?`（選填）；`addPublishedVersion(data, label?, effectiveDate?, note?)` 加第 4 個 `note` 參數（**trim 非空才存**）；`publishedVersionToInfo` 帶入 note
  - **簽章改 options**：`OrgProvider.publishVersion` 由位置參數改為 **options 物件** `(draft, { label?, note?, effectiveDate? })`，回傳新版本 id；`onPublish` 同步改 options；所有呼叫端（含 `OrgChartPage`）一併更新，生效日經 opts 正確傳遞
  - **UI**：`EditModeToolbar` 發布對話框在「生效日」之前新增「**版本名稱（選填，留空＝以發布時間命名）**」與「**這次調整的理由（選填）**」兩欄；R0.1 三題維持 nudge、不持久化。`VersionSelector` 在 active 版本下顯示「**調整理由：…**」
  - 後端零改動（`note` 為本機 metadata、不寫穿雲端，與 best-effort 取捨一致）
- ✅ **Phase 5 R5.3 CSV 欄位中文化**（commit `086e4b3`，規劃見 [規劃-改善Roadmap §6](規劃-改善Roadmap.md)）——CSV 匯入／匯出欄名改「中文為主、英文向後相容」，降低 HR 直接編輯 CSV 的門檻
  - `R5.3` **CSV 欄位中文化**：匯入／匯出欄名改**中文主名**，既有**英文 CSV 仍可匯入**（向後相容）
  - **單一事實來源** `CSV_MEMBER_COLUMN_DEFS`（`src/services/csvToOrgData.ts`，13 欄）：每欄含 `key`（英文 const，型別／匯出／UI 多處沿用不變）／`zh`（中文主名）／`aliases`（至少含英文 key）
  - **header 中英相容解析**：依「中文主名 → 英文 key → 別名」順序找第一個命中欄位（英文比對採 lowercase）；缺欄位錯誤訊息改用**中文欄名**（`csvColumnZh`）；匯出 header 一律輸出中文（`CSV_MEMBER_COLUMNS_ZH`），匯出→再匯入自洽
  - **一併中文化**：範本 `org-members.template.csv`／`org-members.sample.csv`、`generate:csv-sample` 產生腳本、`CsvImportPage` 頁面說明
  - 中文欄名（13 欄）：員工工號／員工姓名／在職狀態／組別代碼／組別名稱／上層組別代碼／組別狀態／職級代碼／職級名稱／職級層級／主管工號／直屬主管工號／是否主要組別
  - 後端零改動
- ✅ **Phase 5 R5.2 編輯態 before/after 指標浮層**（commit `8d72028`，規劃見 [規劃-改善Roadmap §6](規劃-改善Roadmap.md)）——Phase 5 體驗摩擦消除進行中的首項落地
  - `R5.2` **編輯態 before/after 指標浮層**：**匯報視角編輯模式**顯示「本次調整的影響」橫條（新元件 `EditImpactBar`），即時呈現草稿相對基準的 4 指標 before→after——平均管理幅度／最大層級／警示數／規劃就緒度，含 `delta` 與方向（improved／worsened／unchanged）
  - **新增純函式** `compareOrgHealth(base, draft)`（`src/services/orgHealth.ts`）：重用 `buildOrgHealth`／`buildReadiness`，固定 4 指標、回傳 `before`／`after`／`delta`／`direction`／`hasChanges`；前三項越小越好、就緒度越大越好；`avgSpan` 以**四捨五入到小數 1 位**後的值算 before/after/delta/direction，與 UI `toFixed(1)` 顯示精度一致（避免「4.8→4.8 卻標 −0.1」矛盾）
  - 資料源：`OrgChartPage` 匯報視角編輯模式掛載，base＝`session.baseData`、after＝`draftData`
  - 後端零改動
- ✅ **Phase 0 vibe 校準（R0.1–R0.5）**（commit `1bdd955`，契約見 [契約-Phase0-vibe校準](契約-Phase0-vibe校準.md)、規劃見 [規劃-改善Roadmap §1](規劃-改善Roadmap.md)）——把 CM 變革管理思維注入核心流程的五項低成本落地
  - `R0.1` **發布按鈕加摩擦**：`EditModeToolbar` 發布對話框新增三題**選填**提問（sponsor／受影響關鍵人員／sustainment owner），任一空白柔性提醒但不阻擋發布；純前端 nudge，**不寫入 `OrgData`**
  - `R0.2` **詞彙去工程化**：健檢 category「斷鏈→懸空匯報、單點風險→無備援主管」、`BaselineSelector`「捕捉快照→設定比較基準」、orgHealth「孤兒節點→無任何歸屬」、`BpmnImpact`「單點風險→僅一位核准人（無備援）」；BPMN 設計子頁「節點」維持流程語境
  - `R0.3` **健檢結果分群摺疊**：findings 依 category 分群、可摺疊（原生 `details`），減少資訊過載
  - `R0.4` **「保留事項」摘要**：`scenarioCompare` 新增 `retained`（`unchangedRatio` 以**歸屬**為分母），情境比較卡顯示「X% 的人員配置維持不變」，給員工安定感 anchor
  - `R0.5` **規劃就緒度（結構面）**：`orgHealth` 新增 `buildReadiness`（4 結構維度 0–100 + 等級），`/health` 加區塊；誠實命名「結構面就緒度」並連 `/roadmap` Phase 1 註記（完整變革就緒需搭配 stakeholder 評估）
  - 後端零改動
- ✅ **M5 改善 Roadmap 頁面**（commit `d4faec5`，契約見 [契約-Roadmap頁面](契約-Roadmap頁面.md)、規劃見 [規劃-改善Roadmap](規劃-改善Roadmap.md)）
  - **新增頁面 `/roadmap`**：`src/pages/RoadmapPage.tsx`，呈現「**未來改善方向 + 完成進度**」——版面為「頁首 → 狀態總覽橫條（5 狀態 + 合計）→ vibe 警告 callout → 依 phase 分組的 Card → 紅線 callout → 訪談驗證 8 問 callout」
  - **新增純資料 + 純函式服務**：`src/services/roadmap.ts`，hard-coded `RoadmapData`（phases + items + redLines + validationQuestions + lastUpdated），三支 public API——`getRoadmapData()`／`countByStatus(data)`／`groupItemsByPhase(data)`；資料 frozen 避免被誤改。狀態列舉：`done｜in-progress｜planned｜researching｜observing`；來源列舉：`pm｜uxr｜cm｜consensus`
  - **資料內容**：整合 PM／UXR／變革管理顧問三方評審後的改善計畫——Phase 0 vibe 校準（R0.1–R0.5）、Phase 1 Desire/Stakeholder（R1.1–R1.3）、Phase 2 Reinforcement/Sustainment（R2.1–R2.3）、Phase 3 流程衝擊強化（R3.1–R3.3）、Phase 4 規劃模型擴充（R4.1–R4.3）、Phase 5 體驗摩擦消除（R5.1–R5.5）、觀察項（OBS.1–OBS.6）；已完成項目以 commit hash + 完成日期呈現
  - **主導覽更新**：`Layout.tsx` 末尾新增「改善 Roadmap」（icon `Map`，路由 `/roadmap`）——放在「CSV 匯入」之後最末項，屬 meta／治理資訊，與規劃功能分群
  - **同步維護機制**：規劃文件 [規劃-改善Roadmap](規劃-改善Roadmap.md) 是事實來源，`roadmap.ts` 與系統內頁面隨 commit 同步更新；改善項目落地時，同步更新 item 的 `status / commitHash / completedAt` 與規劃文件勾選框，一起 commit
  - 後端零改動
- ✅ **M4 README + 使用者導引**（commit `2368961`）
  - 使用者面 README（給誰、解決什麼、怎麼用）
  - 典型 HR 規劃旅程 walkthrough（載入 → 編輯 → 健檢 → 比較 → 影響 → 發布）
  - 收尾整理（archive 完成項目、清掉死連結）
- ✅ **M3 整合 UX 收尾**（commit `a37e4b2`，契約見 [契約-整合UX收尾](契約-整合UX收尾.md)）
  - **新總覽頁 `OverviewPage`**：新增 `src/pages/OverviewPage.tsx`，路由 `/`（index）；原 `PeoplePage` 搬至 `/people`；版面為「歡迎標題 → 智慧 CTA 卡 → 6 步旅程地圖 → 5 個狀態小卡（在職員工／部門／專案職能／已發布版本／流程定義）」
  - **規劃旅程地圖（6 步固定順序）**：載入 → 編輯 → 健檢 → 比較 → 影響 → 發布；每步顯示 `done`／`ready` 狀態，可直接點擊跳對應路由；「無資料」時後續步驟視覺淡化
  - **智慧 CTA 純函式服務**：新增 `src/services/overviewStatus.ts`，`buildOverviewStatus(args)` 由資料／版本／流程／影響基準狀態推導旅程進度與**建議的下一步**；5 條推薦規則由上往下第一個命中（無資料→`load`、未發布→`health`、僅 1 版本→`compare`、≥2 版本且有流程未設基準→`impact`、其餘→`health`），純函式、無副作用便於單元測試
  - **`publishedVersionsCount` 改用本機計數**：`OverviewPage` 透過 `loadPublishedVersions().length` 取數，避免後端啟用時把雲端版本誤計入引導推薦的「已發布版本數」
  - **主導覽更新**：`Layout.tsx` 新增「總覽」為首項（icon `LayoutDashboard`），「人員與歸屬」改 `/people`；其他導覽項目不變
  - **M2 收尾兩個 nit 一併處理**：
    - `BpmnImpactPage` 引導 callout 文案改為「**主管異動找不到核准人**」（更口語、貼 HR 視角）
    - 當 `store.processes.length === 0` 時，隱藏 Tabs／`BaselineSelector`／頁尾「進階」連結，改顯示主要 CTA 卡片「**還沒有任何流程定義**」+「**前往管理流程定義**」連 `/bpmn`
  - 後端零改動
- ✅ **M2 BPMN 影響分析重定位**（commit `7b81a2a`，契約見 [契約-變更影響重定位](契約-變更影響重定位.md)）
  - **主導覽收斂**：`Layout.tsx` 將 `/bpmn`（「BPMN 流程」、icon `Workflow`）改為 `/bpmn/impact`（「**變更影響**」、icon `TriangleAlert`）；移除未用的 `Workflow` import
  - **`BpmnImpactPage` 文案重定位（HR 視角）**：標題「流程影響分析」→「**變更影響分析**」；副標改為「比較組織調整前後，動到哪些作業／決策流程的核准人與路徑——協助規劃決策」；Tab「即時健檢」→「**流程健檢**」（避免與 `/health` 規劃健檢混淆）；移除「返回 BPMN」按鈕（與其他主導覽頁一致）
  - **HR 友善引導 callout**：Header 下方、`BaselineSelector` 上方新增一段「這頁能回答什麼」的引導，並指引「沒有流程資料時可至頁尾『進階：管理流程定義』設定」
  - **次層入口**：頁尾新增「**進階：管理流程定義 →**」按鈕連 `/bpmn`，BPMN 清單／設計／模擬三頁路由不變、邏輯零改動、書籤不會壞
  - 後端零改動；`processImpact.ts`、`BpmnProvider` 完全不動
- ✅ **M1 規劃情境比較 what-if**（commit `01c29d0`，契約見 [契約-情境比較 what-if](契約-情境比較whatif.md)）
  - `scenarioCompare.ts` 純函式 `buildScenarioComparison(inputs)`：入口先 `backfillAssignmentLevels` 正規化、以陣列首個情境為基準呼叫 `computeOrgDiff(baseline → current)`、各情境跑 `buildOrgHealth`，產出 10 指標的 `metricMatrix`（`warningCount` / `functionsWithoutMembers` / `functionsWithoutLead` / `spofCount` 標 `down`，其餘 `neutral`）與 8 計數的 `diffSummary`
  - `/compare` 頁 + 導覽入口「情境比較」：最多 4 個情境槽，資料源為 `useOrg().dataVersions`（含內建種子、mock、發布版本、雲端版本）；無效版本選項 disabled 並標「✗（格式錯誤）」
  - 互動細節：首槽預設 active 版本；第一個有效槽自動為基準（`baselineSlotIndex`），不在第 0 槽時顯示 callout「目前基準＝第 N 槽（…版本名）」；`slots.length > 1` 時每槽都可刪；至少 2 個有效情境才呈現對照
  - 呈現：情境槽列（含健檢摘要小卡）+ 指標對照表（down 指標最大值 warning／最小值 success，全相同不上色）+ 結構差異摘要卡（非基準情境各一張）
  - 後端零改動
- ✅ **規劃指標／健檢 Org Health**（commit `e9da58d`，契約見 [契約-規劃健檢](契約-規劃健檢.md)）
  - `orgHealth.ts` 純函式 + `/health` 頁；指標（管理幅度、層級深度、職能覆蓋、結構風險 findings）
- ✅ **專案職能雙維度（匯報線 × 職能）**（commit `13f5d4a`，契約見 [契約-專案職能雙維度](契約-專案職能雙維度.md)）
  - `Group.kind`、`functionCoverage.ts`、`FunctionCoveragePanel`、組別歸屬視角過濾

### 基礎能力（已落地）
- ✅ 後端 API + 持久層 P1–P3（opt-in、JSONB 版本持久化、前端寫穿、docker compose 一鍵起整套）
- ✅ Excel 匯入（`xlsxToOrgData`，懶載入）
- ✅ 版本生效日（`effectiveDate`，排程/已生效徽章）
- ✅ 自動化測試與覆蓋率門檻（前端 **463** 測試、後端路由+Prisma 整合測試）
- ✅ 雙視角組織圖、變更歷程、CSV 匯入、版本管理、localStorage 草稿

> 歷史落地報告：[reports/UI測試驗收報告](reports/UI測試驗收報告.md)、[reports/無障礙稽核報告](reports/無障礙稽核報告.md)、[reports/規劃-自動化測試與schema版控](reports/規劃-自動化測試與schema版控.md)、[reports/規劃-後端與持久層](reports/規劃-後端與持久層.md)。

---

## 已降級（偏全公司營運系統，非定位核心）

僅在使用者明確要求時才排。

- 🔻 後端實體表正規化 P4（JSONB 文件夠用，正規化是低使用者價值內部優化）
- 🔻 BPMN 持久化 P5
- 🔻 認證接點 P6 → Azure AD/SSO → 多租戶
- 🔻 正式送簽流程（接 BPMN 簽核）

---

## 暫緩

- ⏸️ [介面規劃 Penpot](設計-介面規劃-penpot.md)（使用者 2026-06-06 指示先別管；spec 留著）
