# 代辦清單（Backlog）

> 由 Claude 在 `integration` 分支逐項完成；每項完成即推送（pre-push 驗證把關）。
> 完成度 = 已完成的「本輪範圍」項目 ÷ 本輪範圍總數。
> 最後更新：2026-06-06（**專案職能雙維度**完成；依校準後定位，偏全公司營運系統的項目降為非主線）。

## 本輪範圍（要逐一完成）

### 測試與覆蓋率
- [x] T1. `dataVersions` 測試（loadDataVersions / pickDefaultVersionId / publishedVersionToInfo / buildLabel / 壞檔處理）
- [x] T2. `publishedVersions` 測試（新增/刪除/去重、bundle 匯出入、壞輸入）
- [x] T3. `exportImport` 剩餘測試（parseOrgDataFile、download* 於 jsdom、cloneOrgData）
- [x] T4. `BpmnProvider` reducer 完整測試（upsert/delete/session/history/baseline）
- [x] T5. `buildGroupMembershipGraph` 深入測試（cluster、外部主管、全公司視角、循環）
- [x] T6. 覆蓋率門檻提升（已達 services ≈ 88%、整體 ≈ 86%；門檻 83/68/86/84）

### 品質與效能
- [x] Q1. vendor chunk 拆分（vite manualChunks：react / reactflow / 其他），確認 build 與分包
- [x] Q2. localStorage 寫入失敗的使用者可見提示（safeSetItem + Layout 警告橫幅）+ 測試

### 文件
- [x] D1. README 增加「測試」章節與指令；同步各文件至最終狀態

## 大型功能（逐一確認後推進）

> 定位校準：本工具為 **HR 規劃／試算工具**（匯報線 × 專案職能）。以下 **HR 規劃主線**優先；偏全公司營運系統的項目（#1 P4、#2、#3、#5 正式送簽）降為**非主線、非定位核心**。

### HR 規劃主線

- [x] **專案職能雙維度（reporting line × function）** — ✅ 完成（commit 13f5d4a，契約見 [契約-專案職能雙維度](契約-專案職能雙維度.md)）
  - `Group.kind`（部門／職能）型別 + migration（ORG_SCHEMA_VERSION 1→2，舊資料回填 department）
  - validators：department 父須為 department、function 無 parentId、主歸屬須為 department
  - `functionCoverage.ts`（職能視角：成員／無成員／無 lead／跨職能負載）+ `buildGroupMembershipGraph` 加 `kindFilter`
  - UI：GroupForm 種類選擇、GroupsPage 種類徽章、OrgChartPage 部門/職能過濾 + `FunctionCoveragePanel`
  - 匯入補 `kind='department'`（CSV/xlsx 同管線）、種子新增 g7 XFN-AI / g8 XFN-OKR 兩職能組
  - 後端無需改（OrgData 以 JSONB round-trip）

### #1 後端 API + 持久層 — 進行中（見 [規劃](規劃-後端與持久層.md)）
> 決策：自建 API + DB、Docker、PostgreSQL；棧：Node + TS + Fastify (+ Prisma)。
- [x] P1 骨架：`server/`（Fastify health）+ docker-compose（Postgres）+ 後端測試 + verify/CI 納入後端，前端不受影響
- [x] P2 版本持久化 API
  - [x] P2a REST 路由 + repository 抽象 + 記憶體實作 + 測試（versions/draft CRUD）
  - [x] P2b Prisma（PostgreSQL）實作 + 整合測試（真實 Postgres）+ CI postgres service
- [x] P3 前端接線
  - [x] P3a API client（apiClient + VITE_API_URL 旗標）+ 測試
  - [x] P3b 接入 OrgProvider（雲端版本 hydrate + 發布/刪除寫穿；後端加 CORS）+ 真實後端瀏覽器驗證
  - [x] docker-compose 納入前端 web 服務（nginx）→ 整套（db + api + web）一鍵啟動
- [ ] P4 正規化實體表（employee/group/jobLevel/assignment/changeEntry）+ 預留 tenantId — **偏全公司營運系統的內部優化，依校準後定位降級／非主線**
- [ ] P5 BPMN 持久化
- [ ] P6 認證接點（銜接 #2）

### 其他大型功能（待逐一確認）
- #2 Azure AD / SSO（需 #1）— **偏全公司營運系統，依校準後定位降級／非主線**
- #3 多租戶（需 #1、#2）— **偏全公司營運系統，依校準後定位降級／非主線**
- [x] #4 Excel 匯入（.xlsx → matrixToOrgData，重用 CSV 管線；@e965/xlsx 懶載入；CsvImportPage 加 Excel 上傳）— 服務單元測試 + 真實瀏覽器 .xlsx 解析驗證
- [~] #5 簽核生效日／正式送簽
  - [x] 版本生效日（effectiveDate）：發布可指定生效日、`pickEffectiveVersionId`/`effectiveStatus` 純邏輯、版本下拉顯示「排程／已生效」— 單元測試 + 瀏覽器 e2e
  - [ ] 正式送簽流程（接 BPMN 簽核）— **偏全公司營運系統，依校準後定位降級／非主線**；需求較大，待與使用者確認

> 備註：#1 P4（DB 正規化）暫緩——JSONB 文件持久化已可用，正規化屬內部優化、低使用者價值、高風險，待使用者面功能完成後再評估。

---

## 進度

- **測試／品質／文件本輪**：9 / 9（100%）✅ 全部完成
- **HR 規劃主線**：
  - 專案職能雙維度（匯報線 × 職能）— ✅ 完成（commit 13f5d4a）
- **大型功能（基礎能力）**：
  - #1 後端 API + 持久層 — **P1–P3 完成**（opt-in 後端、JSONB 版本持久化、前端寫穿、整套 docker compose）；P4–P6 待辦（降為非主線）。
  - #4 Excel 匯入 — ✅ 完成
  - #5 簽核生效日／正式送簽 — 🔶 版本生效日完成；正式送簽（接 BPMN）降為非主線、待與使用者確認
  - #2 SSO、#3 多租戶 — ⏳ 待辦（依賴 #1 P6）；偏營運系統、降為非主線
- 進行中：（無）— 待使用者指定下一項。**HR 規劃導向候選**：規劃指標／健檢、規劃情境比較（what-if）、BPMN 影響分析重定位為 HR 決策輔助透鏡。（營運系統項目 P4／P5／正式送簽／SSO 已降為非主線，僅在使用者明確要求時再排。）
