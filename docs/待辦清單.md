# 代辦清單（Backlog）

> 由 Claude 在 `integration` 分支逐項完成；每項完成即推送（pre-push 驗證把關）。
> 最後更新：2026-06-06（M5 改善 Roadmap 頁面完成；下一個開工：Phase 0 vibe 校準 R0.1–R0.5）。
> 進度：M1 ✅ → M2 ✅ → M3 ✅ → M4 ✅ → M5 ✅ → Phase 0 ⏳。

## 定位（最高指導原則）

本工具為 **HR 規劃／試算工具**（匯報線 × 專案職能），不是全公司營運系統。
BPMN 是組織變更影響的**輔助透鏡**，非簽核引擎。
偏全公司營運系統的項目（送簽、SSO、多租戶、後端正規化）已**降為非主線**。

---

## 下一個開工：Phase 0 vibe 校準（R0.1–R0.5）

M1–M5 收尾路線圖已全部完成。下一個里程碑進入 **[規劃-改善Roadmap](規劃-改善Roadmap.md)** 的 Phase 0——
低成本高槓桿，把 CM（變革管理顧問）思維注入工具核心流程。
完整 Phase 1–5 與觀察項詳見規劃文件；系統內 `/roadmap` 頁面同步呈現。

### Phase 0 — vibe 校準 〔下一個開工〕
CM 核心警告：**「HR 在 30% 完成度時以為自己在 90%」**。工具當前的「規劃 → 一鍵發布 → 結束」流程暗示完成感過早；Phase 0 五項落地後，HR 一進來就不會走錯方向。

- [ ] `R0.1` **發布按鈕加摩擦**：publish 前提問三題（sponsor 誰？stakeholder 名單？sustainment owner？），任一題空白警告但不擋
- [ ] `R0.2` **詞彙稽核**：去工程化全站文案（SPOF→無備援主管、斷鏈→懸空匯報、層級深度→組織層級、節點→人員、邊→匯報線、捕捉快照→設定比較基準、function→跨部門職能、發布→改名留待 Phase 1 決定）
- [ ] `R0.3` **健檢結果分群摺疊**：findings 按類別分群（span/depth/function/chain/cycle/spof），減少資訊過載
- [ ] `R0.4` **「保留事項」摘要**：scenarioCompare 結果加一欄「未變動的部分」（從 diff 反推），給員工 anchor
- [ ] `R0.5` **Readiness Score**：從現有 findings 自動算 100 分量表（Leadership/Capacity/Stakeholder/Infrastructure/Comms 五大類），在 `/health` 加分頁

> Phase 1–5 與觀察項見 [規劃-改善Roadmap](規劃-改善Roadmap.md)；部分項目（Phase 1/2 較大投資）須先訪談 3–5 位真實 HR 再動，題目見規劃文件 §9。

---

## 已完成（按時間倒序，細節在系統設計文件與契約文件）

### HR 規劃主線
- ✅ **M5 改善 Roadmap 頁面**（commit `d4faec5`，契約見 [契約-Roadmap頁面](契約-Roadmap頁面.md)、規劃見 [規劃-改善Roadmap](規劃-改善Roadmap.md)）
  - **新增頁面 `/roadmap`**：`src/pages/RoadmapPage.tsx`，呈現「**未來改善方向 + 完成進度**」——版面為「頁首 → 狀態總覽橫條（5 狀態 + 合計）→ vibe 警告 callout → 依 phase 分組的 Card → 紅線 callout → 訪談驗證 8 問 callout」
  - **新增純資料 + 純函式服務**：`src/services/roadmap.ts`，hard-coded `RoadmapData`（phases + items + redLines + validationQuestions + lastUpdated），三支 public API——`getRoadmapData()`／`countByStatus(data)`／`groupItemsByPhase(data)`；資料 frozen 避免被誤改。狀態列舉：`done｜in-progress｜planned｜researching｜observing`；來源列舉：`pm｜uxr｜cm｜consensus`
  - **資料內容**：整合 PM／UXR／變革管理顧問三方評審後的改善計畫——Phase 0 vibe 校準（R0.1–R0.5）、Phase 1 Desire/Stakeholder（R1.1–R1.3）、Phase 2 Reinforcement/Sustainment（R2.1–R2.3）、Phase 3 流程衝擊強化（R3.1–R3.3）、Phase 4 規劃模型擴充（R4.1–R4.3）、Phase 5 體驗摩擦消除（R5.1–R5.5）、觀察項（OBS.1–OBS.6）；已完成項目以 commit hash + 完成日期呈現
  - **主導覽更新**：`Layout.tsx` 末尾新增「改善 Roadmap」（icon `Map`，路由 `/roadmap`）——放在「CSV 匯入」之後最末項，屬 meta／治理資訊，與規劃功能分群
  - **同步維護機制**：規劃文件 [規劃-改善Roadmap](規劃-改善Roadmap.md) 是事實來源，`roadmap.ts` 與系統內頁面隨 commit 同步更新；改善項目落地時，同步更新 item 的 `status / commitHash / completedAt` 與規劃文件勾選框，一起 commit
  - 後端零改動
- ✅ **M4 README + 使用者導引**（commit `2368961`）
  - 使用者面 README（給誰、解決什麼、怎麼用）
  - 典型 HR 規劃旅程 walkthrough（載入 → 編輯 → 健檢 → 比較 → 影響 → 發布）
  - 收尾整理（archive 完成項目、清掉死連結）
- ✅ **M3 整合 UX 收尾**（commit `a37e4b2`，契約見 [契約-整合UX收尾](契約-整合UX收尾.md)）
  - **新總覽頁 `OverviewPage`**：新增 `src/pages/OverviewPage.tsx`，路由 `/`（index）；原 `PeoplePage` 搬至 `/people`；版面為「歡迎標題 → 智慧 CTA 卡 → 6 步旅程地圖 → 5 個狀態小卡（在職員工／部門／專案職能／已發布版本／流程定義）」
  - **規劃旅程地圖（6 步固定順序）**：載入 → 編輯 → 健檢 → 比較 → 影響 → 發布；每步顯示 `done`／`ready` 狀態，可直接點擊跳對應路由；「無資料」時後續步驟視覺淡化
  - **智慧 CTA 純函式服務**：新增 `src/services/overviewStatus.ts`，`buildOverviewStatus(args)` 由資料／版本／流程／影響基準狀態推導旅程進度與**建議的下一步**；5 條推薦規則由上往下第一個命中（無資料→`load`、未發布→`health`、僅 1 版本→`compare`、≥2 版本且有流程未設基準→`impact`、其餘→`health`），純函式、無副作用便於單元測試
  - **`publishedVersionsCount` 改用本機計數**：`OverviewPage` 透過 `loadPublishedVersions().length` 取數，避免後端啟用時把雲端版本誤計入引導推薦的「已發布版本數」
  - **主導覽更新**：`Layout.tsx` 新增「總覽」為首項（icon `LayoutDashboard`），「人員與歸屬」改 `/people`；其他導覽項目不變
  - **M2 收尾兩個 nit 一併處理**：
    - `BpmnImpactPage` 引導 callout 文案改為「**主管異動找不到核准人**」（更口語、貼 HR 視角）
    - 當 `store.processes.length === 0` 時，隱藏 Tabs／`BaselineSelector`／頁尾「進階」連結，改顯示主要 CTA 卡片「**還沒有任何流程定義**」+「**前往管理流程定義**」連 `/bpmn`
  - 後端零改動
- ✅ **M2 BPMN 影響分析重定位**（commit `7b81a2a`，契約見 [契約-變更影響重定位](契約-變更影響重定位.md)）
  - **主導覽收斂**：`Layout.tsx` 將 `/bpmn`（「BPMN 流程」、icon `Workflow`）改為 `/bpmn/impact`（「**變更影響**」、icon `TriangleAlert`）；移除未用的 `Workflow` import
  - **`BpmnImpactPage` 文案重定位（HR 視角）**：標題「流程影響分析」→「**變更影響分析**」；副標改為「比較組織調整前後，動到哪些作業／決策流程的核准人與路徑——協助規劃決策」；Tab「即時健檢」→「**流程健檢**」（避免與 `/health` 規劃健檢混淆）；移除「返回 BPMN」按鈕（與其他主導覽頁一致）
  - **HR 友善引導 callout**：Header 下方、`BaselineSelector` 上方新增一段「這頁能回答什麼」的引導，並指引「沒有流程資料時可至頁尾『進階：管理流程定義』設定」
  - **次層入口**：頁尾新增「**進階：管理流程定義 →**」按鈕連 `/bpmn`，BPMN 清單／設計／模擬三頁路由不變、邏輯零改動、書籤不會壞
  - 後端零改動；`processImpact.ts`、`BpmnProvider` 完全不動
- ✅ **M1 規劃情境比較 what-if**（commit `01c29d0`，契約見 [契約-情境比較 what-if](契約-情境比較whatif.md)）
  - `scenarioCompare.ts` 純函式 `buildScenarioComparison(inputs)`：入口先 `backfillAssignmentLevels` 正規化、以陣列首個情境為基準呼叫 `computeOrgDiff(baseline → current)`、各情境跑 `buildOrgHealth`，產出 10 指標的 `metricMatrix`（`warningCount` / `functionsWithoutMembers` / `functionsWithoutLead` / `spofCount` 標 `down`，其餘 `neutral`）與 8 計數的 `diffSummary`
  - `/compare` 頁 + 導覽入口「情境比較」：最多 4 個情境槽，資料源為 `useOrg().dataVersions`（含內建種子、mock、發布版本、雲端版本）；無效版本選項 disabled 並標「✗（格式錯誤）」
  - 互動細節：首槽預設 active 版本；第一個有效槽自動為基準（`baselineSlotIndex`），不在第 0 槽時顯示 callout「目前基準＝第 N 槽（…版本名）」；`slots.length > 1` 時每槽都可刪；至少 2 個有效情境才呈現對照
  - 呈現：情境槽列（含健檢摘要小卡）+ 指標對照表（down 指標最大值 warning／最小值 success，全相同不上色）+ 結構差異摘要卡（非基準情境各一張）
  - 後端零改動
- ✅ **規劃指標／健檢 Org Health**（commit `e9da58d`，契約見 [契約-規劃健檢](契約-規劃健檢.md)）
  - `orgHealth.ts` 純函式 + `/health` 頁；指標（管理幅度、層級深度、職能覆蓋、結構風險 findings）
- ✅ **專案職能雙維度（匯報線 × 職能）**（commit `13f5d4a`，契約見 [契約-專案職能雙維度](契約-專案職能雙維度.md)）
  - `Group.kind`、`functionCoverage.ts`、`FunctionCoveragePanel`、組別歸屬視角過濾

### 基礎能力（已落地）
- ✅ 後端 API + 持久層 P1–P3（opt-in、JSONB 版本持久化、前端寫穿、docker compose 一鍵起整套）
- ✅ Excel 匯入（`xlsxToOrgData`，懶載入）
- ✅ 版本生效日（`effectiveDate`，排程/已生效徽章）
- ✅ 自動化測試與覆蓋率門檻（前端 **375** 測試、後端路由+Prisma 整合測試）
- ✅ 雙視角組織圖、變更歷程、CSV 匯入、版本管理、localStorage 草稿

> 歷史落地報告：[reports/UI測試驗收報告](reports/UI測試驗收報告.md)、[reports/無障礙稽核報告](reports/無障礙稽核報告.md)、[reports/規劃-自動化測試與schema版控](reports/規劃-自動化測試與schema版控.md)、[reports/規劃-後端與持久層](reports/規劃-後端與持久層.md)。

---

## 已降級（偏全公司營運系統，非定位核心）

僅在使用者明確要求時才排。

- 🔻 後端實體表正規化 P4（JSONB 文件夠用，正規化是低使用者價值內部優化）
- 🔻 BPMN 持久化 P5
- 🔻 認證接點 P6 → Azure AD/SSO → 多租戶
- 🔻 正式送簽流程（接 BPMN 簽核）

---

## 暫緩

- ⏸️ [介面規劃 Penpot](設計-介面規劃-penpot.md)（使用者 2026-06-06 指示先別管；spec 留著）
