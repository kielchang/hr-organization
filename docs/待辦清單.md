# 代辦清單（Backlog）

> 由 Claude 在 `integration` 分支逐項完成；每項完成即推送（pre-push 驗證把關）。
> 完成度 = 已完成的「本輪範圍」項目 ÷ 本輪範圍總數。
> 最後更新：2026-06-06。

## 本輪範圍（要逐一完成）

### 測試與覆蓋率
- [x] T1. `dataVersions` 測試（loadDataVersions / pickDefaultVersionId / publishedVersionToInfo / buildLabel / 壞檔處理）
- [x] T2. `publishedVersions` 測試（新增/刪除/去重、bundle 匯出入、壞輸入）
- [x] T3. `exportImport` 剩餘測試（parseOrgDataFile、download* 於 jsdom、cloneOrgData）
- [x] T4. `BpmnProvider` reducer 完整測試（upsert/delete/session/history/baseline）
- [x] T5. `buildGroupMembershipGraph` 深入測試（cluster、外部主管、全公司視角、循環）
- [x] T6. 覆蓋率門檻提升（已達 services ≈ 88%、整體 ≈ 86%；門檻 83/68/86/84）

### 品質與效能
- [x] Q1. vendor chunk 拆分（vite manualChunks：react / reactflow / 其他），確認 build 與分包
- [x] Q2. localStorage 寫入失敗的使用者可見提示（safeSetItem + Layout 警告橫幅）+ 測試

### 文件
- [x] D1. README 增加「測試」章節與指令；同步各文件至最終狀態

## 大型功能（逐一確認後推進）

### #1 後端 API + 持久層 — 進行中（見 [規劃](規劃-後端與持久層.md)）
> 決策：自建 API + DB、Docker、PostgreSQL；棧：Node + TS + Fastify (+ Prisma)。
- [x] P1 骨架：`server/`（Fastify health）+ docker-compose（Postgres）+ 後端測試 + verify/CI 納入後端，前端不受影響
- [ ] P2 版本持久化 API（org_version JSONB + draft；versions/draft CRUD）
- [ ] P3 前端接線（API client + `VITE_API_URL` feature flag，localStorage 後備）
- [ ] P4 正規化實體表（employee/group/jobLevel/assignment/changeEntry）+ 預留 tenantId
- [ ] P5 BPMN 持久化
- [ ] P6 認證接點（銜接 #2）

### 其他大型功能（待逐一確認）
- #2 Azure AD / SSO（需 #1）
- #3 多租戶（需 #1、#2）
- #4 Excel 匯入（純前端，可重用 CSV 管線）
- #5 簽核生效日／正式送簽（前端資料模型＋UI）

---

## 進度

- 完成度：**9 / 9（100%）** ✅ 本輪範圍全部完成
- 進行中：（無）— 本輪 backlog 已清空；後續若有新項目再於此追加。
