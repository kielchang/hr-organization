server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;

  # SPA：未命中的路徑回 index.html，交給 react-router 處理
  location / {
    try_files $uri $uri/ /index.html;
  }

  # 靜態資源長快取（檔名帶 hash）
  location /assets/ {
    expires 1y;
    add_header Cache-Control "public, immutable";
  }
}
